// Sample configuration file for API keys
// IMPORTANT: Copy this file to config.js and add your own API keys

const config = {
  mapbox: {
    token: 'YOUR_MAPBOX_TOKEN_HERE'
  },
  gemini: {
    apiKey: 'YOUR_GEMINI_API_KEY_HERE'
  }
};

// Export the configuration
export default config;